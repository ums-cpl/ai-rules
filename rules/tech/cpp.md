---
description: C++ style and conventions (placeholder for future rules)
globs: "**/*.cpp"
alwaysApply: false
---

# C++ Style (Apply **Only** to C++ Content)

## Applicability Gate (MUST be checked before applying)

These rules apply **exclusively** to C++ documents.

Apply this rule **only if at least one condition holds**:
- The file being edited has the extension `.cpp`.
- The content clearly contains C++ syntax (e.g., `#include`, `namespace`, `class`, `template`, etc.).

If the content is **not** C++:
→ **Do not apply these rules. Ignore this document entirely.**

If unsure whether the content is C++:
→ **Do not apply the rules.**

## Precedence

Explicit user instructions override these rules.

## Scope

These rules affect **formatting only**.  
Do **not** rewrite wording, change semantics, or refactor structure.

## Rules

<!-- TODO: Add C++-specific rules (formatting, naming, modern C++, etc.) -->

## Non-Goals (Do NOT do this)

<!-- TODO: Add C++-specific non-goals -->
